// Moved to shared/agentHistory.ts (#138) so the localagent daemon reuses it;
// re-exported here to preserve the module surface for gateway/ and its tests.
export * from "../../shared/agentHistory";
