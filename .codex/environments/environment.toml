# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "setup"

[setup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

# Same contract as the Claude Code WorktreeCreate hook: repos opt in to setup
# by providing an executable scripts/worktree-setup.
SETUP="$CODEX_WORKTREE_PATH/scripts/worktree-setup"
if [[ -x "$SETUP" ]]; then
  SOURCE_DIR="$CODEX_SOURCE_TREE_PATH" WORKTREE_PATH="$CODEX_WORKTREE_PATH" "$SETUP"
elif [[ -e "$SETUP" ]]; then
  echo "worktree setup: $SETUP exists but is not executable" >&2
  exit 1
fi
'''
